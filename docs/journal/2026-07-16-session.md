# Journal — 2026-07-16

## Codex — Phase 8 M4 visualization platform

- บริบท: ผู้ใช้สั่ง `continue` ให้ implement แผน Phase 8 ต่อใน fresh context โดยยึดแผนก่อนหน้าเป็นเจตนา และ AGENTS/CLAUDE ระบุให้ทำตาม Phase 8 scope เท่านั้น ห้ามข้ามไป M5 ก่อนปิด M4
- อ่าน governance/สถานะ: `AGENTS.md`, `CLAUDE.md`, `docs/STATE.md`, `docs/PHASE8-BRIEF.md`; ยืนยันว่า M1-M3 ปิดแล้วและ M4 เป็นงานถัดไป
- ขอบเขตที่ทำ: ปิดเฉพาะ P8-M4 ไม่เริ่ม typed debate moves, verifier, embedding adapter, benchmark หรือ observability ของ P8-M5
- เหตุผลหลักที่อธิบายระหว่างทำงาน:
  - Run Detail ต้องมี cache/reconnect/polling fallback เพราะ SSE เป็น live optimization ส่วน replay event id เป็น source of continuity
  - Visualization ต้องไม่เป็น canvas อย่างเดียว จึงเพิ่ม table fallback และ keyboard-selectable controls เพื่อ accessibility
  - ECharts import แบบทั้ง package ทำให้ bundle ใหญ่เกินจำเป็น จึงเปลี่ยนเป็น modular imports และแยก manual chunks
  - Full pytest timeout รอบแรกเกิดจาก dev stack ไม่ได้รัน (`docker compose ps` ว่าง) จึงเปิด stack ก่อน rerun
- งาน backend/API:
  - เพิ่ม `universe_estimates` ใน dashboard payload เพื่อให้ multiverse range chart แสดงข้อมูลจริง
  - เพิ่มค่า child `value` ใน validation report เพื่อทำ stability matrix และเปรียบเทียบ 3-seed validation
  - เพิ่ม `scripts/export_openapi.py` สำหรับ export FastAPI OpenAPI schema ให้ frontend generated client
- งาน frontend:
  - เพิ่ม dependency และ scripts สำหรับ TanStack Query, ECharts, Cytoscape, OpenAPI generation, Vitest, Playwright
  - เพิ่ม generated typed OpenAPI client ผ่าน `openapi-fetch`
  - หุ้ม app ด้วย `QueryClientProvider`
  - เพิ่ม `useRunEvents` สำหรับ EventSource reconnect ด้วย `after_id`, ignore malformed event และ expose state/lastEventId
  - ปรับ Run Detail ให้ใช้ TanStack Query, typed client, polling fallback ระหว่าง queued/running, validation polling, ECharts charts, Cytoscape contention graph และ evidence lineage Sankey
  - เพิ่ม `RunVisualizations.tsx` สำหรับ multiverse range, scenario comparison, stance timeline/beeswarm, stability matrix, contention graph, evidence lineage และ fallback tables
- งาน test:
  - เพิ่ม Python unit test สำหรับ OpenAPI export
  - เพิ่ม Vitest setup และ unit tests สำหรับ SSE reconnect/malformed event รวมถึง visualization components/table fallbacks/segment interaction
  - เพิ่ม Playwright config และ e2e tests สำหรับ direct deep link `#/runs/e2e-run`, nonblank chart canvas, desktop/mobile responsive fallback และ evidence lineage
  - ติดตั้ง Playwright Chromium binary เพราะเครื่องยังไม่มี browser สำหรับ runner
- Verification ที่รันและผ่าน:
  - `uv run pytest -q` ผ่าน 369 tests หลังเปิด dev stack
  - `uv run ruff check .` ผ่าน
  - `uv run ruff format --check .` ผ่าน
  - `npm.cmd run generate:api` ผ่าน
  - `npm.cmd run test` ผ่าน 4 Vitest tests
  - `npm.cmd run build` ผ่าน และแยก chunks เป็น query/vendor/index/charts
  - `npm.cmd run test:e2e` ผ่าน 4 Playwright tests (chromium + mobile)
  - `docker compose up -d` ผ่านและ build image รวม frontend production build ผ่าน
- ข้อควรระวังส่งต่อ:
  - `.gitignore` modified และ `diagrams/` untracked เป็น user changes เดิม ไม่ได้ stage/commit
  - P8-M5 ยังไม่เริ่ม งานถัดไปคือ typed debate moves/verifier/embedding adapter/benchmarks/observability ตาม brief
  - charts chunk ยังใหญ่ตามธรรมชาติของ ECharts+Cytoscape แต่ถูกแยกออกจาก main index chunk แล้ว

## Codex — Phase 8 M5 Core Engine/Retrieval และปิดเฟส

- บริบท/มติผู้ใช้: ผู้ใช้สั่ง “continue งานต่อ ทำที่เหลือ” หลัง M4 commit แล้ว จึงทำเฉพาะ
  P8-M5 ทั้งห้ารายการตาม brief และไม่เปิด Phase 9; รักษา `.gitignore` modified กับ `diagrams/`
  untracked ซึ่งเป็นงานเดิมของผู้ใช้ไว้ไม่แตะ
- อ่าน governance/architecture ใหม่: `AGENTS.md`, `CLAUDE.md`, STATE, PHASE8 brief,
  TECH-DECISIONS และ ADR-0001/0007/0008/0010/0011; ยึด LLM adapter เดียว, BudgetGuard,
  migration-only DDL, provenance/reproducibility, Thai-first และ no-raw-PII telemetry
- Typed Debate:
  - เพิ่ม move contract `claim`, `evidence`, `counterclaim`, `concession`, `question`
    พร้อม deterministic `move_id`, `parent_move_id`, `evidence_refs`; prompt/JSON Schema และ DB snapshot
    รองรับ lineage โดย snapshot เก่าไม่ถูกแก้และอ่านเป็น `legacy_unverifiable`
  - เพิ่ม deterministic verifier ตรวจ unique/schema, parent ต้องมาก่อน, evidence ID, evidence move ไม่มี
    citation, unsupported numeric claim, concession/contradiction; เก็บ violations ดิบและ lineage graph
  - analyst synthesis call เดิมทำหน้าที่ judge ด้วย เพื่อลด call/cost; judge ครบ citation,
    contradiction, unsupported claim และ schema assessment และมี deterministic severity floor กัน analyst
    ลดระดับ verifier finding
- Reflection:
  - opt-in เท่านั้น, เก็บใน run เดียว ไม่เขียน long-term memory; policy จำกัด 2 calls, input 2,400 chars,
    output 220 tokens และทุก call ใช้ analyst tier ผ่าน adapter/BudgetGuard
  - เพิ่ม offline paired smoke harness เพื่อพิสูจน์ bounds/measurement path; รายงานชัดว่า fixture ที่กำหนด
    ล่วงหน้าไม่พิสูจน์ causal efficacy ของ reflection
- Retrieval:
  - เพิ่ม embedding model/dimension ใน Settings/env และ `LLMAdapter.embed()` ที่คิดราคา fail-closed,
    เช็ก marginal cost ก่อน provider call, บันทึก model version/dimension และรวม actual cost ใน run/monthly spend
  - เปลี่ยน monthly spend ledger ของ debate เป็นบันทึกต่อ provider call หลังได้ usage จริง เพื่อให้งานที่
    ล้มเหลวหรือ call ที่ชน runtime cap ยังถูกนับต้นทุน; ตัด aggregate post-run write เดิมเพื่อไม่ให้นับซ้ำ
  - เพิ่ม migration pgvector table, HNSW expression index สำหรับ 1536d; dimension อื่นทำ exact pgvector
    พร้อม provenance ไม่อ้างว่า HNSW
  - แก้ BM25 ให้ tokenize Thai character trigrams เมื่อไม่มีเว้นวรรค; hybrid ใช้ Reciprocal Rank Fusion
    ระหว่าง BM25/vector rank; embedding ไม่ตั้ง/ล้ม = explicit `bm25_fallback_embedding_unavailable`
  - vector retrieval ตรวจ coverage ก่อน query; ถ้า index สำเร็จเพียงบาง chunk จะไม่ใช้ผล vector บางส่วน
    แต่ fallback BM25 พร้อม `embedding_coverage_incomplete` เพื่อไม่ให้ ranking ดูสมบูรณ์เกินจริง
- Benchmark:
  - เพิ่ม fixture ไทยและ runner ครอบ retrieval, evidence relevance, subgroup fidelity,
    social-desirability, future-event calibration และ reflection bounds
  - ผลดิบ: retrieval n=2 Recall@5/MRR=1.0; evidence n=2 unsupported=1; subgroup n=3
    MAE=.013333; social n=2 direction accuracy=1.0; future calibration n=4 Brier=.0825,
    baseline=.25, skill=.67, **ECE=.275**; เก็บข้อจำกัด/sample size ตรง ๆ ใน
    `docs/reports/P8-M5-benchmarks.md` ไม่มี threshold ย้อนหลัง
- Observability:
  - เพิ่ม manual OpenTelemetry spans + trace header propagation API→Celery→retrieval→LLM/embedding;
    optional OTLP HTTP endpoint
  - เพิ่ม Prometheus counters/histograms ที่ `/metrics`, provider call metadata table และ
    `/observability.json`; metadata มี provider/operation/tier/status/latency/tokens/cost/error kind/model
    เท่านั้น ไม่เก็บ prompt, response, secret หรือ PII
  - Insights มี queue/provider health, success rate, latency, cost และ failure taxonomy; Settings ตั้ง
    embedding model/dimension ได้ และ New Run มี retrieval provenance note + reflection toggle
- Migration ที่เพิ่ม:
  - `2026-07-16-typed-debate-moves-v1`
  - `2026-07-16-vector-retrieval-observability-v1` (เป็น latest startup schema gate)
- Dependency ที่เพิ่มตามแผนที่ผู้ใช้อนุมัติ: OpenTelemetry API/SDK/OTLP HTTP exporter และ
  `prometheus-client`; ไม่มีการพลิก ADR/TECH decision จึงไม่สร้าง ADR ใหม่
- Verification ที่ผ่านก่อนส่งมอบ:
  - backend full suite 377 tests (warning เดิม FastAPI/TestClient เรื่อง httpx2 หนึ่งรายการ)
  - ruff check/format ทั้ง repo
  - OpenAPI generation, Vitest 4 tests, production frontend build, Playwright desktop/mobile 4 tests
  - benchmark offline runner และ migration runner no-op
- ข้อจำกัดส่งต่อ:
  - embedding ไม่ hardcode model; ต้องตั้ง model+ราคา+dimension ก่อนจึงใช้ vector จริง ไม่ตั้งจะ fallback BM25
  - HNSW optimized path ตอนนี้คือ 1536d; dimension อื่นยังถูกต้องแต่เป็น exact scan และบอก provenance
  - benchmark ปัจจุบันเป็น harness ขนาดเล็ก ไม่ใช่ external validation; future calibration ต้องสะสม
    prediction ที่ resolve โลกจริงและ qrels ไทยที่ human-label ต่อ
  - Phase 8 M1-M5 ปิดแล้ว ณ จุดส่งมอบนั้น; session ถัดมาผู้ใช้สั่งปิดรายการ platform ที่เหลือเป็น M6

## Codex — Phase 8 M6 Experiment workspace และ platform completion

- บริบท/มติผู้ใช้: หลัง M5 ถูกปิด ผู้ใช้สั่ง “continue ทำที่เหลือต่อทั้งหมด” โดยแผนต้นทางยังมี
  experiment workspace, API modularization, discriminated frontend contracts และ scale test 1,000 agents
  ที่ยังไม่มี implementation จึงเพิ่ม P8-M6 ใน brief เพื่อปิดงานเหล่านี้ภายใน Phase 8 เดิม ไม่เปิด Phase 9
- การตัดสินใจที่ใช้:
  - workspace อ่านผลจาก stored run snapshots เท่านั้นเพื่อ reproducibility และไม่ rerun analyst ระหว่าง compare
  - sweep จำกัด 12 variants; ตรวจค่าใช้จ่ายทุกรายการและยอดรวมเทียบ monthly cap ก่อน enqueue รายการแรก
    เพื่อลด partial execution จากงบไม่พอ (ยังคง BudgetGuard ใน worker เป็นด่านที่สอง)
  - public gallery votes ไม่ถูกใช้เป็น parameter/input และ API ตอบ `public_votes_used=false` ชัดเจน
  - ไม่เพิ่ม runtime DDL; schema ใหม่เข้าผ่าน migration ledger/advisory lock เดิมเท่านั้น
- Backend/API:
  - เพิ่ม `experiment_workspaces` และ `experiment_members` พร้อม store create/list/get/add/delete
  - เพิ่ม service expand/preflight/analyze สำหรับ seed/agents/rounds/red-team/retrieval/reflection ตาม engine;
    reject parameter ที่ engine ไม่ใช้และ variant ที่เกิน cap แทนการรันแบบให้ความหมายผิด
  - เพิ่ม endpoints list/detail/compare/sweep/delete; arbitrary comparison ตรวจ run ids ก่อนสร้าง workspace
  - เพิ่ม shared `RunBody` ใน `api/models.py`, แยก ops endpoints และ experiment endpoints/service ออกจาก
    `api/app.py`; Fabric persistent run ส่ง requested seed เข้า multiverse จริงและบันทึก experiment lineage
  - migration latest เป็น `2026-07-16-experiment-workspaces-v1` และ startup gate อ่าน version นี้
- Frontend:
  - เพิ่ม `#/experiments` ใน sidebar, TanStack Query list/detail polling, comparison form, bounded sweep form,
    sensitivity ranking/table, cost/failure status และป้ายยืนยันว่า public votes ไม่ถูกใช้
  - เพิ่ม discriminated `SimRunDetail`/`RunPayload` สำหรับ Fabric กับ Debate และ regenerate OpenAPI schema
  - เพิ่ม `VirtualizedDebateList` ที่ render เฉพาะ viewport/overscan สำหรับ 1,000 posts พร้อม ARIA list metadata
  - E2E รอบแรก timeout ก่อนเปิด Debate tab; ตรวจพบต้นเหตุไม่ใช่ feed แต่เป็น contention graph ที่สร้าง
    คู่ edges O(n²) จาก 1,000 unique segments จึงจำกัด interactive graph ที่ 24 segment เด่น ขณะที่ feed
    และ snapshot ยังคง posts ครบ พร้อมข้อความบอกผู้ใช้เมื่อมี segment ถูกย่อในกราฟ
- Tests/verification:
  - เพิ่ม backend 6 tests สำหรับ sweep bounds/ignored dimensions/aggregate budget/requested Fabric seed/
    stored comparison/sensitivity/API queue behavior
  - เพิ่ม Vitest 1 เคส payload 1,000 posts และ Playwright experiment deep link/arbitrary compare + Debate
    1,000 posts; Playwright รันทั้ง Chromium desktop/mobile รวม 8 tests
  - full backend ผ่าน 383 tests (warning เดิม FastAPI/TestClient), ruff check/format ผ่าน
  - OpenAPI generation, Vitest 5 tests, production build และ Playwright 8 tests ผ่าน
  - migration runner รายงาน schema current; PostgreSQL/Redis/Neo4j healthy
- ข้อจำกัดที่บันทึกตรงไปตรงมา:
  - aggregate budget preflight ไม่ใช่ ledger reservation; worker BudgetGuard ยังเป็น authority หากมี run อื่นใช้
    monthly budget พร้อมกันหลัง preflight
  - รอบนี้แยก ops/experiment/shared models ซึ่งเป็นกลุ่มที่แก้ใน M6; legacy endpoint กลุ่มอื่นใน `api/app.py`
    ยังไม่ถูกย้ายแบบ big-bang เพื่อไม่ขยาย regression surface
  - `.gitignore` modified และ `diagrams/` untracked เป็นงานผู้ใช้เดิม ไม่ถูก stage/revert/commit

## Codex — Phase 8 M7 production hardening หลังผู้ใช้สั่ง “ทำต่อได้เลย”

- ผู้ใช้อนุญาตให้ทำ technical debt/production readiness ต่อจากรายการส่งมอบ M6 จึงเพิ่ม M7 ภายใน
  Phase 8 เดิม ไม่เปิด Phase 9 และรักษา `.gitignore`/`diagrams/` ของผู้ใช้ไว้
- Budget reservation:
  - เพิ่ม `monthly_budget_reservations`; batch reserve ใช้ PostgreSQL transaction advisory lock เดียวกับ
    การตัดสินงบ ทำให้สอง sweep พร้อมกันไม่สามารถเห็นวงเงินคงเหลือก้อนเดียวกันแล้วผ่านทั้งคู่
  - Experiment และ 3-seed validation preallocate run ids, reserve ทุก child ก่อน enqueue ตัวแรก; queue failure
    คืน reservation ของรายการที่ยังไม่ถูกส่ง
  - LLM adapter settle actual spend กับ reservation และ `_run_create_impl` release ส่วนเหลือใน `finally`
    ทั้ง success/error; monthly cap ยังมี per-run BudgetGuard เป็นด่านแยก
- API/frontend:
  - แยก Settings GET/PUT/secret endpoints ไป `api/routers/settings.py`, contract เดิม และเพิ่ม
    `reserved_this_month`/`available_this_month` โดยไม่ส่ง secret/ciphertext
  - main run readiness/create/async/job/history, Settings และ Experiments ใช้ generated OpenAPI client
  - React routes lazy-load; initial index JS ลดจาก ~248 kB เป็น ~82 kB, charts 642 kB และ graph 444 kB
    เป็น chunks ที่โหลดเมื่อใช้; เพิ่ม Vite dev proxy ให้ครอบ API paths จริง
- Production/security readiness:
  - เพิ่ม CLI profiles self-hosted/public-ga; ตรวจ auth/key length/PII/watermark/master key/default DB passwords
    โดย output ไม่มีค่า secret; public-ga block TLS/pen-test/OIDC/RLS
  - เพิ่ม lock export + `pip-audit -r` และ `npm audit` ใน CI; local ไม่พบ known vulnerability ใน
    dependency โดย `chimlang` เองถูก skip ตามคาดเพราะเป็น local package ไม่อยู่ PyPI
  - research primary source พบ MIRACL Thai มี human qrels/corpus และ license เปิด เหมาะเป็น external retrieval
    gate; บันทึกใน report แต่ไม่อ้าง benchmark result เพราะยังไม่ได้ download/pin/run corpus จริง
  - ADR-0012 เขียนเป็น Proposed ณ session นั้น (ภายหลังถูก Deferred): ขอผู้ใช้เลือก Caddy vs org load balancer, OIDC issuer/claims
    และ PostgreSQL RLS/default-tenant migration ก่อน implement; ยังไม่สร้าง pseudo multi-tenant จาก header
- Verification: backend 387 tests, ruff, OpenAPI generation, Vitest 5, production build, Playwright 8
  desktop/mobile, npm/pip audit และ migration no-op ผ่าน; warning เดิมมีเพียง FastAPI/TestClient httpx2

## Codex — บันทึก Future Work ตามคำสั่งผู้ใช้

- ผู้ใช้สั่งเก็บ 3 งานจาก ADR-0012 ไว้ทำภายหลังและขอรายละเอียดเพื่อกลับมาอ่านเข้าใจ ได้แก่
  TLS termination, enterprise OIDC SSO และ PostgreSQL RLS multi-tenant isolation
- สร้าง `docs/FUTURE-WORK.md` เป็น source of truth โดยแต่ละงานมี current limitation, เป้าหมาย,
  ตัวเลือก/ข้อมูลที่ต้องเตรียม, implementation หรือ migration checklist, adversarial tests และ acceptance
  criteria; ระบุ dependency/order ว่า tenant/role contract ต้องนิ่งก่อน OIDC mapping และ TLS ต้องพร้อมก่อน
  เปิด OIDC callback ต่อผู้ใช้จริง
- เปลี่ยน ADR-0012 จาก Proposed เป็น Deferred โดยไม่ถือว่าคำแนะนำ Caddy/OIDC/RLS เป็นมติ architecture
- ทำ inventory เพิ่มจาก PRD/STATE/security review:
  - public-GA blockers: independent pen-test, distributed rate limit/auth audit, backup/restore/DR,
    operational SLO/alerts และ legal/ethics readiness
  - trust evidence: MIRACL Thai run จริง, future-event calibration, TRUST-08 panel, population calibration,
    field/pilot evidence
  - engineering debt: แยก API monolith ต่อ, raw fetch ที่เหลือ, optional chart optimization,
    httpx2 warning และ authenticated production soak test
  - business/product decisions: metering model, open-source/license, election eligibility และ semantic memory gate
- ไม่มีการแก้ production code ใน session นี้; `.gitignore`/`diagrams/` ของผู้ใช้ยังไม่แตะ

## Codex — Phase 8 M8 engineering debt และ active business policy

### สิ่งที่ผู้ใช้ขอและมติที่ใช้

- ผู้ใช้ให้ทำ engineering debt ก่อน ได้แก่แยก `api/app.py` ต่อ, ย้าย raw fetch ราว 37 จุด,
  production soak และแก้ warning httpx2; พร้อมให้จัดการ pricing/metering, open-source strategy,
  สิทธิ์ Election Mode และเกณฑ์ก่อน semantic memory แล้วเปิดเว็บให้ทดลอง
- เพื่อไม่เลือก commercial/legal direction เกินอำนาจ จึงใช้ active safe defaults ที่รักษาความสามารถเดิม:
  cost observability แต่ไม่ billing, repository private/no redistribution, Election เฉพาะ verified admin และ
  aggregate-only, semantic memory ปิดและคง bounded run-local reflection จนผ่าน evidence gate
- เขียน ADR-0013 ให้ baseline เหล่านี้เป็นมติที่อ่านได้และบังคับว่าการขยายต้องมี ADR/มติใหม่; public GA
  TLS/OIDC/RLS ยัง Deferred ตาม ADR-0012

### งาน implementation

- ย้าย Persona Pack endpoints ไป `api/routers/personas.py` และ Watchlist/Alert endpoints ไป
  `api/routers/watchlists.py`; คง route, request/response behavior, RBAC, Election guard, PII/budget behavior เดิม
- เพิ่ม `core/product_policy.py` และ read-only authenticated `GET /product-policy.json`; หน้า Settings แสดง
  policy version, active default, rationale และ change gate โดยไม่มีปุ่ม mutation
- regenerate OpenAPI และเปลี่ยน request ทั้งหมดใน `web/src/api.ts` ให้ผ่าน generated `apiClient`;
  `rg "fetch\(" web/src/api.ts` ไม่พบ raw fetch เหลือ
- เพิ่ม dev dependency `httpx2` ตาม Starlette TestClient backend และลบ warning suppression; คง `httpx`
  runtime เพราะ MCP/news/soak ยังใช้
- เพิ่ม `scripts/production_soak.py`: บังคับอย่างน้อย 20 runs และ concurrency ≥2, ใช้ Fabric zero-LLM,
  queue/poll/detail ผ่าน HTTP จริง, ตรวจ heartbeat/event id uniqueness/read-after-write, cleanup และคืน exit 1
  เมื่อมี failure พร้อม unit tests สำหรับ percentile/report

### ปัญหาที่พบระหว่างทำและเหตุผล

- frontend build ใน restricted sandbox ผ่าน TypeScript แล้วแต่ esbuild spawn ถูก EPERM จึงรัน production build
  ภายใต้ approval; build ผ่าน ไม่ใช่ code failure
- worker เริ่มต้นได้รับ stale Redis task เก่าสองรายการจาก session ก่อน: หนึ่งงาน idempotent สำเร็จและหนึ่งงาน
  หา run ที่ถูกลบแล้วไม่พบ; หลัง queue ระบาย worker ready และ soak ใหม่ 20 งานไม่พบ failure/stale/deadlock
- production soak ใช้ Fabric เพื่อวัด runtime/queue/schema โดยไม่ใช้ API provider และไม่กระทบงบ $50/เดือน;
  paid Debate 1,000-agent/TLS/OIDC/SSE disconnect tests ถูกเก็บเป็น future release-scale soak

### ผลตรวจและสถานะส่งมอบ

- live soak: 20/20 ผ่าน, failed=0, p50=1.657s, p95=2.214s, heartbeat coverage=100%,
  event coverage=100%, 6 events/run, missing/duplicate event id=0; cleanup ทั้ง 20 runs
- backend 391 tests, ruff check/format, migration no-op, OpenAPI generation, Vitest 5, production build และ
  Playwright desktop/mobile 8 ผ่าน
- เปิด FastAPI ที่ `http://127.0.0.1:8000/app/` และ Celery worker queues
  `fabric,debate,maintenance` โดยล้าง proxy environment; services PostgreSQL/Redis/Neo4j healthy
- อัปเดต Phase 8 brief ปิด M8, STATE/FUTURE-WORK และรายงาน `docs/reports/P8-M8-production-soak.md`
- `.gitignore` modified และ `diagrams/` untracked เป็นของผู้ใช้ ไม่ stage/revert/commit
