# Journal — 2026-07-16

## Codex — Phase 8 M4 visualization platform

- บริบท: ผู้ใช้สั่ง `continue` ให้ implement แผน Phase 8 ต่อใน fresh context โดยยึดแผนก่อนหน้าเป็นเจตนา และ AGENTS/CLAUDE ระบุให้ทำตาม Phase 8 scope เท่านั้น ห้ามข้ามไป M5 ก่อนปิด M4
- อ่าน governance/สถานะ: `AGENTS.md`, `CLAUDE.md`, `docs/STATE.md`, `docs/PHASE8-BRIEF.md`; ยืนยันว่า M1-M3 ปิดแล้วและ M4 เป็นงานถัดไป
- ขอบเขตที่ทำ: ปิดเฉพาะ P8-M4 ไม่เริ่ม typed debate moves, verifier, embedding adapter, benchmark หรือ observability ของ P8-M5
- เหตุผลหลักที่อธิบายระหว่างทำงาน:
  - Run Detail ต้องมี cache/reconnect/polling fallback เพราะ SSE เป็น live optimization ส่วน replay event id เป็น source of continuity
  - Visualization ต้องไม่เป็น canvas อย่างเดียว จึงเพิ่ม table fallback และ keyboard-selectable controls เพื่อ accessibility
  - ECharts import แบบทั้ง package ทำให้ bundle ใหญ่เกินจำเป็น จึงเปลี่ยนเป็น modular imports และแยก manual chunks
  - Full pytest timeout รอบแรกเกิดจาก dev stack ไม่ได้รัน (`docker compose ps` ว่าง) จึงเปิด stack ก่อน rerun
- งาน backend/API:
  - เพิ่ม `universe_estimates` ใน dashboard payload เพื่อให้ multiverse range chart แสดงข้อมูลจริง
  - เพิ่มค่า child `value` ใน validation report เพื่อทำ stability matrix และเปรียบเทียบ 3-seed validation
  - เพิ่ม `scripts/export_openapi.py` สำหรับ export FastAPI OpenAPI schema ให้ frontend generated client
- งาน frontend:
  - เพิ่ม dependency และ scripts สำหรับ TanStack Query, ECharts, Cytoscape, OpenAPI generation, Vitest, Playwright
  - เพิ่ม generated typed OpenAPI client ผ่าน `openapi-fetch`
  - หุ้ม app ด้วย `QueryClientProvider`
  - เพิ่ม `useRunEvents` สำหรับ EventSource reconnect ด้วย `after_id`, ignore malformed event และ expose state/lastEventId
  - ปรับ Run Detail ให้ใช้ TanStack Query, typed client, polling fallback ระหว่าง queued/running, validation polling, ECharts charts, Cytoscape contention graph และ evidence lineage Sankey
  - เพิ่ม `RunVisualizations.tsx` สำหรับ multiverse range, scenario comparison, stance timeline/beeswarm, stability matrix, contention graph, evidence lineage และ fallback tables
- งาน test:
  - เพิ่ม Python unit test สำหรับ OpenAPI export
  - เพิ่ม Vitest setup และ unit tests สำหรับ SSE reconnect/malformed event รวมถึง visualization components/table fallbacks/segment interaction
  - เพิ่ม Playwright config และ e2e tests สำหรับ direct deep link `#/runs/e2e-run`, nonblank chart canvas, desktop/mobile responsive fallback และ evidence lineage
  - ติดตั้ง Playwright Chromium binary เพราะเครื่องยังไม่มี browser สำหรับ runner
- Verification ที่รันและผ่าน:
  - `uv run pytest -q` ผ่าน 369 tests หลังเปิด dev stack
  - `uv run ruff check .` ผ่าน
  - `uv run ruff format --check .` ผ่าน
  - `npm.cmd run generate:api` ผ่าน
  - `npm.cmd run test` ผ่าน 4 Vitest tests
  - `npm.cmd run build` ผ่าน และแยก chunks เป็น query/vendor/index/charts
  - `npm.cmd run test:e2e` ผ่าน 4 Playwright tests (chromium + mobile)
  - `docker compose up -d` ผ่านและ build image รวม frontend production build ผ่าน
- ข้อควรระวังส่งต่อ:
  - `.gitignore` modified และ `diagrams/` untracked เป็น user changes เดิม ไม่ได้ stage/commit
  - P8-M5 ยังไม่เริ่ม งานถัดไปคือ typed debate moves/verifier/embedding adapter/benchmarks/observability ตาม brief
  - charts chunk ยังใหญ่ตามธรรมชาติของ ECharts+Cytoscape แต่ถูกแยกออกจาก main index chunk แล้ว
